# PLAN

Activity:
Reframe the repo docs so `LODESTONE.md` is the primary scientific entry point
and the wall/H1 material is explicitly supporting baseline machinery.

Question:
Which documents still center the repo on the dyadic wall story instead of the
scale-equivariance thesis?

Next actions:
- update top-level orientation docs to point to `LODESTONE.md` first
- reorganize hypotheses so `L1`-`L3` are primary and `H1` is supporting
- reposition wall and experiment docs as diagnostic/operator support for the
  lodestone program
- leave code or driver renames for the later cycle that actually adds
  partition-comparison experiments

Open decisions:
- whether to add a new `lodestone_sweep.sage` driver or extend
  `h1_sweep.sage` once uniform-partition support exists

Kill when done: yes
